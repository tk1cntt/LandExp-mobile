query SessionQuery($token: String) {
  getSession(token: $token) {
    id
    login
    email
    authorities
    error {
      type
      title
      status
      detail
      path
      message
    }
  }
}

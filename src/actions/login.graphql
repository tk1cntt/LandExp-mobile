query LoginQuery($username: String!, $password: String!) {
  login(username: $username, password: $password) {
    id_token,
    error {
      type
      title
      status
      detail
      path
      message
    }
  }
}
